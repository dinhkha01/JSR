export interface IUser {
  id: number;
  name: string;
  sex: boolean;
  date: string;
  address: string;
}
export interface Product {
  id: number;
  name: string;
  price: string;
  quantity: string;
}
