import React from "react";
import { Outlet } from "react-router-dom";

const Products = () => {
  return (
    <div>
      <h1>tui ne</h1>
      <Outlet />
    </div>
  );
};

export default Products;
